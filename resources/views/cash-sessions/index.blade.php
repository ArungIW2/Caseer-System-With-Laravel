<!DOCTYPE html>
<html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Kasir — Caseer</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-100 text-slate-900"><main class="mx-auto max-w-7xl px-6 py-8"><div class="flex items-center justify-between"><div><a href="{{ route('dashboard') }}" class="text-sm text-indigo-600">← Dashboard</a><h1 class="mt-2 text-2xl font-bold">Sesi Kasir</h1></div><a href="{{ route('cash-sessions.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Buka Sesi</a></div>
@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif
<div class="mt-6 grid gap-4 md:grid-cols-2">@foreach($openSessions as $open)<div class="rounded-xl border border-amber-200 bg-amber-50 p-5"><p class="text-xs uppercase text-amber-700">Sesi terbuka</p><p class="mt-1 font-bold">{{ $open->session_number }}</p><p class="text-sm">{{ $open->store->name }}</p><a href="{{ route('cash-sessions.close', $open) }}" class="mt-4 inline-block rounded-lg bg-amber-600 px-4 py-2 text-sm font-semibold text-white">Tutup & Rekonsiliasi</a></div>@endforeach</div>
<div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Sesi</th><th class="p-4">Store</th><th class="p-4">Dibuka</th><th class="p-4">Status</th><th class="p-4">Expected</th><th class="p-4"></th></tr></thead><tbody>@forelse($sessions as $session)<tr class="border-t"><td class="p-4 font-medium">{{ $session->session_number }}</td><td class="p-4">{{ $session->store->name }}</td><td class="p-4">{{ $session->opened_at }}</td><td class="p-4">{{ $session->status }}</td><td class="p-4">Rp {{ number_format($session->expected_cash,2,',','.') }}</td><td class="p-4 text-right"><a class="text-indigo-600" href="{{ route('cash-sessions.show',$session) }}">Detail</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">Belum ada sesi.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $sessions->links() }}</div></main></body></html>
