<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ ucfirst($action) }} Inventory</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="min-h-screen bg-slate-50 text-slate-900"><div class="mx-auto max-w-2xl p-6"><a href="{{ route('inventory.index') }}" class="text-sm text-indigo-600">← Kembali ke inventory</a><div class="mt-4 rounded-lg bg-white p-6 shadow-sm ring-1 ring-slate-200"><h1 class="mb-6 text-2xl font-bold">{{ ['receive'=>'Stock In','issue'=>'Stock Out','adjust'=>'Stock Adjustment','opname'=>'Stock Opname','transfer'=>'Transfer Antar Toko'][$action] }}</h1>@if($errors->any())<div class="mb-4 rounded border border-red-200 bg-red-50 p-3 text-red-800"><ul class="list-disc pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="post" action="{{ route('inventory.store',$action) }}" class="space-y-4">@csrf
<label class="block text-sm font-medium">Toko Asal<select name="store_id" required class="mt-1 w-full rounded border p-2">@foreach($stores as $store)<option value="{{ $store->id }}">{{ $store->name }}</option>@endforeach</select></label>
<label class="block text-sm font-medium">Produk<select name="product_id" required class="mt-1 w-full rounded border p-2">@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->sku }} — {{ $product->name }}</option>@endforeach</select></label>
@if($action === 'adjust')<label class="block text-sm font-medium">Saldo Baru<input type="number" name="new_quantity" min="0" step="0.001" required class="mt-1 w-full rounded border p-2"></label>@elseif($action === 'opname')<label class="block text-sm font-medium">Hasil Fisik<input type="number" name="counted_quantity" min="0" step="0.001" required class="mt-1 w-full rounded border p-2"></label>@else<label class="block text-sm font-medium">Jumlah<input type="number" name="quantity" min="0.001" step="0.001" required class="mt-1 w-full rounded border p-2"></label>@endif
@if($action === 'receive')<label class="block text-sm font-medium">Harga Pokok / Unit<input type="number" name="unit_cost" min="0" step="0.01" class="mt-1 w-full rounded border p-2"></label>@endif
@if($action === 'transfer')<label class="block text-sm font-medium">Toko Tujuan<select name="to_store_id" required class="mt-1 w-full rounded border p-2">@foreach($stores as $store)<option value="{{ $store->id }}">{{ $store->name }}</option>@endforeach</select></label>@endif
<label class="block text-sm font-medium">Alasan / Catatan<textarea name="reason" rows="3" class="mt-1 w-full rounded border p-2" placeholder="Opsional"></textarea></label><button class="w-full rounded bg-indigo-600 px-4 py-2 font-medium text-white">Simpan Perubahan</button></form></div></div></body></html>
