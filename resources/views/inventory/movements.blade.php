<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Inventory Ledger</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="min-h-screen bg-slate-50 text-slate-900"><div class="mx-auto max-w-7xl p-6"><div class="mb-5 flex items-center justify-between"><div><h1 class="text-2xl font-bold">Inventory Movement Ledger</h1><p class="text-sm text-slate-500">{{ $store->name }}</p></div><a class="rounded border bg-white px-4 py-2 text-sm" href="{{ route('inventory.index',['store_id'=>$store->id]) }}">Inventory</a></div><form class="mb-4 flex gap-2" method="get"><input type="hidden" name="store_id" value="{{ $store->id }}"><select name="movement_type" class="rounded border p-2"><option value="">Semua movement</option>@foreach(['in'=>'Stock In','out'=>'Stock Out','adjustment_in'=>'Adjustment In','adjustment_out'=>'Adjustment Out','opname_in'=>'Opname In','opname_out'=>'Opname Out','transfer_in'=>'Transfer In','transfer_out'=>'Transfer Out'] as $key=>$label)<option value="{{ $key }}" @selected(request('movement_type')===$key)>{{ $label }}</option>@endforeach</select><button class="rounded bg-indigo-600 px-4 py-2 text-white">Filter</button></form><div class="overflow-x-auto rounded-lg bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead class="bg-slate-100"><tr><th class="p-3 text-left">Waktu</th><th class="p-3 text-left">Produk</th><th class="p-3 text-left">Tipe</th><th class="p-3 text-right">Qty</th><th class="p-3 text-right">Sebelum</th><th class="p-3 text-right">Sesudah</th><th class="p-3 text-left">User</th><th class="p-3 text-left">Catatan</th></tr></thead><tbody>@forelse($movements as $movement)<tr class="border-t"><td class="p-3">{{ $movement->occurred_at?->format('d/m/Y H:i') }}</td><td class="p-3">{{ $movement->product->name }}</td><td class="p-3">{{ $movement->movement_type }}</td><td class="p-3 text-right">{{ number_format((float)$movement->quantity,3) }}</td><td class="p-3 text-right">{{ number_format((float)$movement->quantity_before,3) }}</td><td class="p-3 text-right font-semibold">{{ number_format((float)$movement->quantity_after,3) }}</td><td class="p-3">{{ $movement->user?->name ?? 'System' }}</td><td class="p-3">{{ $movement->reason }}</td></tr>@empty<tr><td colspan="8" class="p-8 text-center text-slate-500">Belum ada movement.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $movements->links() }}</div></div></body></html>
