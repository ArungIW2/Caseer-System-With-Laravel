<!DOCTYPE html>
<html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Purchasing — Caseer System</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-100 text-slate-900"><main class="mx-auto max-w-7xl px-6 py-8">
<div class="flex items-center justify-between"><div><a href="{{ route('dashboard') }}" class="text-sm text-indigo-600">← Dashboard</a><h1 class="mt-2 text-2xl font-bold">Purchasing</h1><p class="text-slate-600">Purchase order/invoice dan penerimaan barang.</p></div><a href="{{ route('purchases.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">+ Purchase Baru</a></div>
@if(session('success'))<div class="mt-6 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif
<form class="mt-6 flex flex-wrap gap-3 rounded-2xl bg-white p-4 shadow-sm">@if($stores->count()>1)<select name="store_id" class="rounded-lg border p-2"><option value="">Semua Store</option>@foreach($stores as $store)<option value="{{ $store->id }}" @selected($storeId==$store->id)>{{ $store->name }}</option>@endforeach</select>@endif<select name="status" class="rounded-lg border p-2"><option value="">Semua Status</option><option value="draft">Draft</option><option value="received">Received</option></select><button class="rounded-lg border px-4 py-2">Filter</button></form>
<div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Nomor</th><th class="p-4 text-left">Supplier</th><th class="p-4 text-left">Store</th><th class="p-4 text-left">Tanggal</th><th class="p-4 text-left">Status</th><th class="p-4 text-right">Total</th></tr></thead><tbody>@forelse($purchases as $purchase)<tr class="border-t"><td class="p-4"><a class="font-semibold text-indigo-600" href="{{ route('purchases.show',$purchase) }}">{{ $purchase->purchase_number }}</a></td><td class="p-4">{{ $purchase->supplier->name }}</td><td class="p-4">{{ $purchase->store->name }}</td><td class="p-4">{{ $purchase->purchased_at->format('d/m/Y H:i') }}</td><td class="p-4"><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">{{ strtoupper($purchase->status) }}</span></td><td class="p-4 text-right">Rp {{ number_format($purchase->grand_total,2,',','.') }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">Belum ada pembelian.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $purchases->links() }}</div></main></body></html>
