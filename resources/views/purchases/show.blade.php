<!DOCTYPE html>
<html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>{{ $purchase->purchase_number }} — Caseer System</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-100"><main class="mx-auto max-w-6xl px-6 py-8"><a href="{{ route('purchases.index') }}" class="text-sm text-indigo-600">← Purchasing</a><div class="mt-2 flex items-start justify-between"><div><h1 class="text-2xl font-bold">{{ $purchase->purchase_number }}</h1><p class="text-slate-600">{{ $purchase->supplier->name }} · {{ $purchase->store->name }}</p></div><span class="rounded-full bg-slate-200 px-4 py-2 text-sm font-semibold">{{ strtoupper($purchase->status) }}</span></div>@if(session('success'))<div class="mt-6 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif
<div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Produk</th><th class="p-4 text-right">Qty</th><th class="p-4 text-right">Harga</th><th class="p-4 text-right">Diskon</th><th class="p-4 text-right">Pajak</th><th class="p-4 text-right">Total</th></tr></thead><tbody>@foreach($purchase->items as $item)<tr class="border-t"><td class="p-4">{{ $item->product->name }}</td><td class="p-4 text-right">{{ $item->quantity }}</td><td class="p-4 text-right">Rp {{ number_format($item->unit_cost,2,',','.') }}</td><td class="p-4 text-right">Rp {{ number_format($item->discount,2,',','.') }}</td><td class="p-4 text-right">Rp {{ number_format($item->tax,2,',','.') }}</td><td class="p-4 text-right">Rp {{ number_format($item->line_total,2,',','.') }}</td></tr>@endforeach</tbody></table></div>
<div class="mt-6 ml-auto max-w-sm rounded-2xl bg-white p-6 shadow-sm space-y-2"><div class="flex justify-between"><span>Subtotal</span><b>Rp {{ number_format($purchase->subtotal,2,',','.') }}</b></div><div class="flex justify-between"><span>Diskon</span><b>Rp {{ number_format($purchase->discount_total,2,',','.') }}</b></div><div class="flex justify-between"><span>Pajak</span><b>Rp {{ number_format($purchase->tax_total,2,',','.') }}</b></div><div class="flex justify-between border-t pt-2 text-lg"><span>Total</span><b>Rp {{ number_format($purchase->grand_total,2,',','.') }}</b></div></div>
@if($purchase->status==='draft')<form method="POST" action="{{ route('purchases.receive',$purchase) }}" class="mt-6">@csrf<button class="rounded-lg bg-emerald-600 px-5 py-3 font-semibold text-white" onclick="return confirm('Terima pembelian dan tambahkan stok?')">Terima Barang & Tambah Stok</button></form>@endif</main></body></html>
