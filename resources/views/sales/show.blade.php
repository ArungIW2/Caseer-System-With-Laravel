<!DOCTYPE html>
<html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>{{ $sale->invoice_number }} — Caseer System</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="bg-slate-100"><main class="mx-auto max-w-3xl px-6 py-8"><div class="mb-4 flex justify-between"><a href="{{ route('sales.index') }}" class="rounded-lg border bg-white px-4 py-2 text-sm">Kembali</a><button onclick="window.print()" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Cetak Struk</button></div><article class="rounded-2xl bg-white p-6 shadow-sm"><div class="text-center"><p class="font-bold text-indigo-600">CASEER SYSTEM</p><h1 class="mt-1 text-xl font-bold">{{ $sale->store->name }}</h1><p class="text-sm text-slate-500">{{ $sale->invoice_number }} · {{ $sale->sold_at->format('d/m/Y H:i') }}</p></div><div class="mt-6 space-y-3">@foreach($sale->items as $item)<div class="flex justify-between gap-4"><div><p class="font-medium">{{ $item->product->name }}</p><p class="text-xs text-slate-500">{{ $item->quantity }} × Rp {{ number_format($item->unit_price,0,',','.') }}</p></div><p class="font-semibold">Rp {{ number_format($item->line_total,0,',','.') }}</p></div>@endforeach</div><div class="mt-6 space-y-2 border-t pt-4 text-sm"><div class="flex justify-between"><span>Subtotal</span><span>Rp {{ number_format($sale->subtotal,0,',','.') }}</span></div><div class="flex justify-between"><span>Diskon</span><span>Rp {{ number_format($sale->discount_total,0,',','.') }}</span></div><div class="flex justify-between"><span>Pajak</span><span>Rp {{ number_format($sale->tax_total,0,',','.') }}</span></div><div class="flex justify-between text-lg font-bold"><span>Total</span><span>Rp {{ number_format($sale->grand_total,0,',','.') }}</span></div><div class="flex justify-between"><span>Dibayar</span><span>Rp {{ number_format($sale->paid_total,0,',','.') }}</span></div><div class="flex justify-between text-emerald-700"><span>Kembalian</span><span>Rp {{ number_format($sale->change_total,0,',','.') }}</span></div></div><p class="mt-6 text-center text-xs text-slate-500">Kasir: {{ $sale->cashier->name }} · Terima kasih.</p></article></main></body></html>
